# Google OAuth2 Configuration Template for Railway Deployment
# Add these to your Railway environment variables (not .env file)

# Required Google OAuth2 Configuration
GOOGLE_CLIENT_ID=383011377067-tk10hrprcnmpkf1oqitmh2jasi8oh3it.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=https://your-railway-app.railway.app/oauth/callback

# Optional Configuration
GOOGLE_CALENDAR_ID=primary
GOOGLE_TOKEN_ENCRYPTION_KEY=your_32_char_encryption_key_here

# Database (automatically configured by Railway)
DATABASE_URL=postgresql://...

# Note: No credentials.json or token.json files needed
# Tokens are stored encrypted in PostgreSQL database
# Complete OAuth flow at /oauth/start endpoint after deployment
